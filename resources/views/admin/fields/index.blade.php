@extends('layouts.admin')
@section('title','Quản lý sân')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="pp-section-title">Quản lý sân bóng</h1><p class="text-muted mb-0">Thêm, sửa, ngừng hoạt động và quản lý hình ảnh sân.</p></div><a class="btn btn-pp" href="{{ route('admin.fields.create') }}">+ Thêm sân</a></div><form class="pp-card p-3 mb-4" method="GET"><div class="input-group"><input class="form-control" name="keyword" value="{{ $keyword }}" placeholder="Tìm theo tên hoặc địa chỉ"><button class="btn btn-pp">Tìm kiếm</button></div></form><div class="pp-card overflow-hidden"><div class="table-responsive"><table class="table table-pp mb-0"><thead><tr><th>Sân</th><th>Địa chỉ</th><th>Giá</th><th>Giờ hoạt động</th><th>Đơn đặt</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($fields as $field)<tr><td><div class="d-flex align-items-center gap-2"><img src="{{ $field->main_image_url }}" style="width:64px;height:48px;object-fit:cover;border-radius:.5rem"><strong>{{ $field->name }}</strong></div></td><td>{{ $field->address }}</td><td>{{ $field->formatted_price }}</td><td>{{ substr($field->open_time,0,5) }} - {{ substr($field->close_time,0,5) }}</td><td>{{ $field->bookings_count }}</td><td><span class="badge {{ $field->status->value==='active'?'text-bg-success':'text-bg-secondary' }}">{{ $field->status->label() }}</span></td><td class="text-nowrap"><a class="btn btn-sm btn-light" href="{{ route('admin.fields.show',$field) }}">Xem</a> <a class="btn btn-sm btn-outline-pp" href="{{ route('admin.fields.edit',$field) }}">Sửa</a></td></tr>@empty<tr><td colspan="7" class="text-center py-4">Chưa có sân bóng.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $fields->links() }}</div>@endsection
