@extends('layouts.admin')
@section('title','Quản lý khung giờ')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="pp-section-title">Quản lý khung giờ</h1><p class="text-muted mb-0">Thiết lập các khoảng thời gian có thể đặt sân.</p></div><a class="btn btn-pp" href="{{ route('admin.time-slots.create') }}">+ Thêm khung giờ</a></div><div class="pp-card overflow-hidden"><div class="table-responsive"><table class="table table-pp mb-0"><thead><tr><th>Bắt đầu</th><th>Kết thúc</th><th>Thời lượng</th><th>Booking</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($timeSlots as $slot)<tr><td><strong>{{ substr($slot->start_time,0,5) }}</strong></td><td><strong>{{ substr($slot->end_time,0,5) }}</strong></td><td>{{ $slot->duration_minutes }} phút</td><td>{{ $slot->bookings_count }}</td><td><span class="badge {{ $slot->status->value==='active'?'text-bg-success':'text-bg-secondary' }}">{{ $slot->status->label() }}</span></td><td class="text-nowrap"><a class="btn btn-sm btn-outline-pp" href="{{ route('admin.time-slots.edit',$slot) }}">Sửa</a> <form class="d-inline" data-confirm="Bạn chắc chắn muốn xóa hoặc tạm ngừng khung giờ này?" method="POST" action="{{ route('admin.time-slots.destroy',$slot) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Xóa</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-4">Chưa có khung giờ.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $timeSlots->links() }}</div>@endsection
