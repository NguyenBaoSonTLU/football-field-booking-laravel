@extends('layouts.app')

@section('title', 'Thông báo')

@section('content')
<section class="pp-section pt-4"><div class="container"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="pp-section-title">Thông báo</h1><p class="text-muted mb-0">Các cập nhật liên quan đến đơn đặt sân và hệ thống.</p></div>@if(auth()->user()->unreadNotifications()->exists())<form method="POST" action="{{ route('notifications.read-all') }}">@csrf @method('PATCH')<button class="btn btn-outline-pp">Đánh dấu tất cả đã đọc</button></form>@endif</div><div class="d-grid gap-3">@forelse($notifications as $notification)<article class="pp-card p-4 {{ $notification->read_at ? '' : 'border-success' }}"><div class="d-flex justify-content-between gap-3"><div><h2 class="h5 fw-bold">{{ data_get($notification->data,'title','Thông báo') }}</h2><p class="mb-2">{{ data_get($notification->data,'message') }}</p><small class="text-muted">{{ $notification->created_at->diffForHumans() }}</small></div>@if(!$notification->read_at)<form method="POST" action="{{ route('notifications.read',$notification->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-light">Đã đọc</button></form>@endif</div>@if(data_get($notification->data,'booking_id'))<a class="d-inline-block mt-3" href="{{ route('bookings.show',data_get($notification->data,'booking_id')) }}">Xem đơn đặt sân →</a>@endif</article>@empty<x-empty-state title="Chưa có thông báo" message="Các cập nhật mới sẽ xuất hiện tại đây." />@endforelse</div><div class="mt-4">{{ $notifications->links() }}</div></div></section>
@endsection
